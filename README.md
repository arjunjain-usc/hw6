HW6 – Hash Tables and Boggle

This assignment implements:
- A custom string hash function (hash.h)
- A hash table with linear and double hashing (ht.h)
- A recursive Boggle solver (boggle.cpp)

Compilation:
make

The code is intended to be compiled with:
g++ -g -Wall -std=c++11

